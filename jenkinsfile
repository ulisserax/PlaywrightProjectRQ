pipeline {
   
   stages {
        stage('runing test') {
                        
            steps {
                // Depends on your language / test framework
                sh 'npm install'
                sh 'npm run env:stage'
            }
        }
    }
}