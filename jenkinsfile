pipeline {
   agent any
   tools { nodejs "default" }
   stages {
        stage('runing test') {
                        
            steps {
                // Depends on your language / test framework
                sh 'node -v'
                sh 'npm -v'
                sh 'npm install'
                sh 'npm run env:stage'
            }
        }
    }
}